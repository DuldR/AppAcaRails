<h1> Here's the idiot baby you picked.</h1>
<%=  render 'header'%>
<section>
    <nav>
        <%= link_to 'Edit Cat', edit_cat_url %>
    </nav>
</section>

<section>

    <br>
    <br>

    <table>
        <thead>

            <tr>
                <th colspan="2"><%= @cat.name %> </th>
            </tr>

            <tbody>
                <tr>
                    <td>Age:</td>
                    <td><%= @cat.age %> </td>
                </tr>

                <tr>
                    <td>Sex:</td>
                    <td><%= @cat.sex %> </td>
                </tr>

                <tr>
                    <td>Color: </td>
                    <td><%= @cat.color %></td>

                </tr>

                <tr>

                    <td>Desctriptiopn: </td>
                    <td><%= @cat.description %>

                </tr>

            </tbody>


        </thead>

    </table>

    <% @rentals.each do |rental| %>
        <%= rental.start_date %>
        <%= rental.end_date %>
    <% end %>

</section>