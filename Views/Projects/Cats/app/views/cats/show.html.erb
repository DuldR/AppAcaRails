<h1> Here's the idiot baby you picked.</h1>
<%=  render 'header'%>
<section>
    <nav>
        <%= link_to 'Edit Cat', edit_cat_url %>
    </nav>
</section>

<section>

    <br>
    <br>

    <table>
        <thead>

            <tr>
                <th colspan="2"><%= @cat.name %> </th>
            </tr>

            <tbody>
                <tr>
                    <td>Age:</td>
                    <td><%= @cat.age %> </td>
                </tr>

                <tr>
                    <td>Sex:</td>
                    <td><%= @cat.sex %> </td>
                </tr>

                <tr>
                    <td>Color: </td>
                    <td><%= @cat.color %></td>

                </tr>

                <tr>

                    <td>Desctription: </td>
                    <td><%= @cat.description %>

                </tr>

            </tbody>


        </thead>

    </table>

    <br>

    <% @rentals.each do |rental| %>
        <div>
            <%= rental.start_date %>
            <%= rental.end_date %>

            <% if rental.pending? %>

                <form action="<%= approve_cat_rental_request_url(rental) %>" method="post">
                    <input type="radio" id="approve" name="cat_rental_request[status]" value="APPROVED">
                    <label for="approve">Approve</label>
                    <input type="radio" id="deny" formaction="<%= deny_cat_rental_request_url(rental) %>" name="cat_rental_request[status]" value="DENIED">
                    <label for="deny">Deny</label>


                    <input type="submit" value="SEND IT">
                </form>
    
            <% end %>
        <div>
    <% end %>

</section>