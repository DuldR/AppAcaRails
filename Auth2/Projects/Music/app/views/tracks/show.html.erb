<section>

    <%= @track.ord %>
    <br>
    <%= @track.title %>
    <br>
    <%= @track.lyrics %>
    <br>
    <%= @track.bonus == true ? "Bonus Track" : "" %>


</section>

<%= render "notes/note" %>
<%= render "notes/form", :track => @track %>

<%= link_to "Edit Track", edit_track_url(@track) %>